# mealbook

## Contributing

### Devcontainer

Make sure your machine has docker installed. Use one of:

- Open in VS Code in a devcontainer
- use the `devcontainer` CLI tool
- use DevPod

### Static site

Run:

```bash
cd /site
pnpm install
pnpm dev
```
